------------------[ DONE ]------------------
* basic variable format
* basic file writing
* dummy token list for testing purposes
* dummy text input buffer with test text
		||
		VV

* read text input buffer from file
		||
		VV

* text result buffer (use a linked list of
* t_nodes for dynamic resizing)
		||
		VV

* code for converting between the two
		||
		VV

* full file-writing capabilities
		||
		VV

------------------[ TODO ]------------------

* implement full list of tokens (two parts:
* two arrays with full list of token
* values and then separate string luts
* for each language, showing which lang
* tokens map to which real tokens)
		||
		VV

* argument flags and other misc features
* (ignore comments flag, etc)
		||
		VV

* implement program to plaintext
		||
		VV

* add preprocessor directives (#include,
* #define, and #if [#else] #end)
